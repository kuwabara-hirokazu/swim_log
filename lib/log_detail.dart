import 'package:flutter/material.dart';

class LogDetail extends StatelessWidget {
  const LogDetail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('記録詳細'),
      ),
      body: const Text('記録詳細'),
    );
  }
}
